package main

func main() {
	var m = map[string]string{}
	var n = map[string]int{}
	var o = map[string]struct{ x, y float64 }{}
	var p = map[string]interface{}{}

	var memoized = map[uint]uint{
		0: 0,
		1: 1,
	}

	var memoized = map[uint]uint{0: 0, 1: 1}

	var memoized = map[uint]uint{}

	a := m[0]
}
