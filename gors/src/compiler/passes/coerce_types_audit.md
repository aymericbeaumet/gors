# coerce_types hardcode audit

Date: 2026-06-03

Scope: production matches from:

```bash
rtk rg -n "fmt|strconv|reflect|strings|sort|unicode|encoding|archive|cmp|TODO|hack|special|hardcode" gors/src/compiler gors/src/resolve gors-builtin/src
```

The intent is to replace package/function-name coercions with compiler facts:
expected Go type, call signature, receiver semantics, lvalue/rvalue role, and
runtime primitive ownership contracts. Tests and ordinary Rust names such as
`std::fmt`, `sort_unstable`, and diagnostics are not tracked here.

## Categories

- `runtime primitive`: generated support for Go builtins, runtime contracts, or
  host resources.
- `generated-language rule`: a generic lowering/ownership rule already expressed
  in terms of Rust or Go shapes rather than a stdlib symbol.
- `stdlib workaround`: package/function-name logic that should be replaced by a
  semantic compiler rule.
- `formatting artifact`: generated-format cleanup or report/debug wording.

## coerce_types pass modules

| Area | Current trigger | Category | Generic rule to implement |
| --- | --- | --- | --- |
| Reflection fallback pruning in `structural_helpers/reflection_fallback.rs` | `reflect` AST path/type-path checks and generated `reflect::Value` self-field metadata | stdlib workaround | Represent reflect/type-switch support as compiler/runtime semantic facts; prune only unreachable IR/control-flow branches, not branches selected by generated token text. |
| Format flush insertion in `structural_helpers/fmt_flush.rs` | generated `__gors_flush_fmt` hook source-field metadata plus receiver self-call graph | stdlib workaround | Flush side effects should be represented as method/lowering semantics for receiver-buffer aliasing, or removed by correctly modeling the buffer alias. |

## Other production hardcodes

| File | Current trigger | Category | Generic rule to implement |
| --- | --- | --- | --- |
| `gors/src/compiler/runtime_primitives/reflect.rs` | `reflect` module replacement in post-prune helpers | runtime primitive | Reflect support is currently a runtime primitive boundary; keep isolated until generic reflect IR/runtime support exists. |
| `gors/src/compiler/runtime_primitives/sync.rs` | `sync.Pool` module replacement in post-prune helpers | runtime primitive | Pooling is modeled as a minimal runtime primitive for host allocation reuse; keep it isolated and avoid package consumer rewrites. |
| `gors/src/compiler/runtime_primitives/os.rs` | `os.Stdout`/`os.File` host-resource replacement | runtime primitive | Host resources may be injected, but must preserve unrelated compiled stdlib items. |
| `gors/src/compiler/reflect_kind.rs` | import-path-aware `reflect.TypeOf(x).Kind() == reflect.K` detection | runtime primitive | This is a reflect runtime boundary; future work should expose it as IR reflect-kind operation instead of AST pattern matching. |
| `gors/src/compiler/reflect_slice_any.rs` | writeback planning for addressable slices passed to `any` parameters, used by reflectlite `Swapper` paths | runtime primitive | Keep this isolated until reflect-backed slice mutation is modeled as generic aliasing/interface lowering. |
| `gors/src/resolve/runtime_primitives.rs` | synthetic `runtime` and `internal/reflectlite` module generation before ordinary resolver parsing | runtime primitive | Keep these as isolated resolver-level runtime primitives until generic runtime/reflectlite lowering can compile the ordinary packages. |
| `gors/src/resolve/structural_helpers/mut_ref_forwarders.rs` and `fmt_flush.rs` | generated mutable-reference trait forwarding impls and injected receiver `__gors_flush_fmt` hooks | stdlib workaround | Interface implementation and receiver-buffer aliasing should be produced by generic method/interface lowering, not resolver post-processing. |
| `gors-builtin/src/lib.rs` | predeclared print/println, interface, reflect-kind helpers | runtime primitive | Builtin language/runtime support is valid, but must not implement stdlib package behavior. |

## Replacement order

1. Replace `structural_helpers.rs` reflection pruning and fmt flush insertion
   with semantic reflect support and receiver-buffer aliasing.
2. Resolver/compiler post-prune fmt helper removal after receiver-buffer aliasing
   is represented semantically.

## Completed removals

| Area | Replacement |
| --- | --- |
| Rust-name-driven `len`/`cap` post-pass casts in `coerce_types.rs` | Builtin lowering now emits Go `int` casts only for actual predeclared `len`/`cap` calls, so user functions or shadowed identifiers named `len`/`cap` are not rewritten. |
| `Box::new` field clone in `value_materialization.rs` | The broad postpass rewrite was removed. Interface field copies are handled by typed interface boxing through generated `__gors_clone_box`, and arbitrary `Box::new(field)` calls are left unchanged. |
| `builtin::append` first/second args in `value_materialization.rs` | The postpass module was removed. Append source values now clone addressable non-Copy lvalues by default, assignment lowering turns same-lvalue append updates into `std::mem::take`, and append elements are cloned from Go element type/addressability facts. |
| integer-typed float local initializer repair in `coerce_types.rs` | Local `var` lowering already compiles explicit Go types through expected-type expression lowering; a compiler regression now pins `var max int = 1e6` to `(1e6 as isize)`. |
| monolithic `coerce_types.rs` visitor responsibilities | Pointer cells, structural helpers, evaluation order, call arguments, binary comparisons, and shared syntax predicates are split into focused modules under `coerce_types/`; the former value-materialization postpass has been folded back into typed lowering. |
| `sort.Slice*` custom lowering in `compiler/stdlib_workarounds.rs` | The workaround module was removed. `sort.Slice*` now compile through ordinary stdlib code using generic `any` call-value copying, function-value callbacks, reflectlite slice swapper support, and writeback for addressable slices passed through `any`. |
| `strconv` string value argument cloning in `coerce_types.rs` | Cross-module cloneable-value call analysis now clones path, field, and index arguments according to the callee's generated `String`/cloneable value parameter types. |
| `slices::Sort` mutable argument borrowing in `coerce_types.rs` | Cross-module mutable-reference call analysis now borrows arguments according to generated callee `&mut` parameter types. |
| non-append `unicode/utf8` value argument cloning in `coerce_types.rs` | Cross-module cloneable-value call analysis now clones `String` and `Vec<u8>` path, field, and index arguments according to generated callee parameter types. |
| `write`/`writeString` method value argument cloning in `coerce_types.rs` | Receiver-qualified method call analysis now clones path, field, and index arguments according to the resolved receiver type and generated method `String`/cloneable value parameter types. |
| `argNumber` second method value argument cloning in `coerce_types.rs` | Receiver-qualified method call analysis now applies the same signature-driven cloneable value argument rule to non-first method arguments. |
| `parsenum`/`getField` function value argument cloning in `coerce_types.rs` | Cross-module cloneable-value call analysis now handles these generated helper calls according to their generated `String`/cloneable value parameter types. |
| `Write` method slice-to-`Vec<u8>` argument coercion in `coerce_types.rs` | Receiver-qualified method call analysis now materializes range-index slice arguments with `.to_vec()` when the resolved method signature expects a `Vec<T>` value parameter. |
| mutable byte-slice calls over sliced pointer fields in `coerce_types.rs` | Signature-driven mutable slice argument rewriting now looks through parenthesized cloned field blocks inside slice indexes and borrows the original lvalue, so `[]byte` writers mutate backing storage instead of cloned temporaries. |
| stale `fmtsort::Sort` argument cloning in `coerce_types.rs` | The package-specific branch was removed; current generated calls are handled by generic call-signature borrowing and cloneable-value analysis. |
| stale `reflect::TypeOf` argument borrowing in `coerce_types.rs` | The package-specific branch and its private borrow helpers were removed; current supported generated paths do not require name-selected `TypeOf` borrowing. |
| stale `reflect::ValueOf` argument coercion in `coerce_types.rs` | The package-specific branch and helper were removed; current supported generated paths prune the reflection fallback before this name-selected coercion is needed. |
| `intFromArg` local argument move in `coerce_types.rs` | Cross-module value-argument analysis now treats by-value `Vec<Box<dyn Any>>` parameters as non-cloneable lvalue takes, driven by the callee signature rather than helper name. |
| `unicode/utf8.AppendRune` receiver argument move in `coerce_types.rs` | Cross-module value-argument analysis now treats by-value `Vec<T>` parameters passed dereferenced lvalues as lvalue takes, driven by callee signature and lvalue role rather than function name or receiver spelling. |
| local initializer cloning by `value`/`f`/`fmtFlags` names in `coerce_types.rs` | Compiler lowering already clones binding initializers through IR addressability and Go type copy semantics; the postpass no longer clones locals solely by identifier or field name. |
| dead `printArg` unsupported-format pruning in `coerce_types.rs` | The name-selected branch had no active predicate; source-level constant branch pruning and reflection fallback pruning now run without a `printArg`-specific no-op path. |
| stale `printValue` argument coercion in `coerce_types.rs` | Current generated supported paths prune reflection fallback calls before method-argument coercion; the postpass no longer rewrites arbitrary `printValue` calls based on field names like `Key` or `Value`. |
| stale `printArg(err)` boxing in `coerce_types.rs` | Current generated supported paths do not need local-name-selected `err` boxing; `Box<dyn Any>` argument materialization should come from expected-type lowering instead of local variable names. |
| stale `printArg(index)` replacement in `coerce_types.rs` | Current generated supported paths lower `[]any` indexes through the generic `clone_any` binding path; the postpass no longer rewrites arbitrary indexed `printArg` arguments by method name. |
| `printArg(self.arg)` empty-any replacement in `coerce_types.rs` | Empty-interface field reads and `any`-to-`any` assignments now copy through the generic `clone_any` runtime helper; the postpass no longer rewrites arbitrary `printArg(self.arg)` calls by method and field name. |
| stale `err = w` rewrite in `coerce_types.rs` | Current generated supported paths do not need local-name-selected extraction from `w.err`; assigning interface values from concrete pointer cells is handled by compiler/interface lowering instead of local variable names. |
| `self.arg = arg` empty-any replacement in `coerce_types.rs` | Empty-interface assignment now copies through generic `clone_any`; the postpass no longer replaces arbitrary `self.arg = arg` assignments by field and local name. |
| `strconv.AppendFloat` call-site lowering and `builtin::append_float` | `strconv` now compiles through the ordinary stdlib path after generic reachable-declaration recovery, scoped local type facts, shared-lvalue parenthesization, typed-constant arithmetic, and folded constant index lowering. The dead runtime helper was removed. |
| `self.value = value` cloning in `coerce_types.rs` | Expected-type expression lowering already clones addressable same-type non-Copy RHS values from Go type facts; the postpass no longer clones arbitrary assignments by field and local name. |
| rendered-token matching in `coerce_types.rs` body/pruning triggers | Remaining body replacement and reflection-pruning gates now use `syn` AST visitors for identifiers, methods, fields, expression paths, and type paths. String literals and formatting cannot accidentally trigger generated-body rewrites or reflection pruning. |
| broad `reflect` segment matching in `structural_helpers.rs` | Reflection fallback pruning now recognizes actual `reflect::...` / `crate::reflect::...` module paths instead of pruning any local identifier or type path segment named `reflect`. |
| broad `reflect` module fallback pruning | Reflection fallback pruning now removes only generated fallback `reflect::ValueOf`/`reflect::TypeOf` expression paths and `reflect::Value` type paths instead of dropping arbitrary `reflect::...` module references. |
| `self.value` reflection fallback pruning in `structural_helpers.rs` | Receiver fallback pruning now derives the prunable self fields from struct fields typed as `reflect::Value`, instead of assuming any field literally named `value` is a generated reflection fallback. |
| flush insertion receiver gates in `structural_helpers.rs` | Flush insertion now records the actual generated flush-trigger methods per receiver with a `__gors_flush_fmt` hook, so statement rewriting uses collected receiver metadata instead of an independent receiver-name plus method-name check. |
| `self.printValue` / `self.fmtPointer` reflection pruning in `structural_helpers.rs` | Reflection fallback pruning no longer drops receiver method calls solely by method name. It prunes actual reflect paths and reflect-typed self fields, then uses dependency-aware block pruning so locals removed from fallback paths also remove their consumers. |
| duplicated flush insertion loops in `coerce_types.rs` and `structural_helpers.rs` | Both passes now call `Metadata::push_stmt_with_flush`, keeping the flush decision and emitted hook call in one structural-helper boundary. |
| top-level-only fmt flush statement matching | Flush insertion now scans immediate statement expressions for receiver calls that write through the generated buffered source, so local initializers and assignments flush without requiring the entire statement to be a `self.method(...)` call. Nested blocks remain owned by their own block rewrite to avoid duplicate flushes. |
| stale `print_arg` names in reflection fallback pruning internals | Reflection pruning helpers are named for their actual fallback-pruning responsibility instead of the older `printArg` call-site workaround. |
| mixed fmt/reflection structural-helper metadata | `Metadata` now delegates to `FmtFlushMetadata` and `ReflectionFallbackMetadata`, so flush insertion and reflection fallback pruning keep separate collection and query responsibilities. |
| fmt-flush metadata controlling reflection fallback activation | Reflection fallback pruning now activates from receiver methods that actually accept `reflect.Value` arguments, so fmt-flush receiver metadata no longer decides whether self-field reflect fallbacks are pruned. |
| fmt flush metadata mixed into `structural_helpers.rs` | Flush-source detection, receiver self-call expansion, and flush insertion now live in `structural_helpers/fmt_flush.rs`; the parent structural helper module keeps only post-helper orchestration plus reflection fallback pruning. |
| reflection fallback pruning mixed into `structural_helpers.rs` | Reflection fallback metadata, reflect-path detection, and dependency pruning now live in `structural_helpers/reflection_fallback.rs`; the parent structural helper module is a post-helper orchestration boundary. |
| local binding/use scanning mixed into reflection fallback pruning | Structural-helper local-name binding and use detection now lives in `structural_helpers/local_names.rs`, leaving reflection fallback pruning responsible only for reflect fallback policy and dependency dropping. |
| scope-blind fallback-local dependency pruning | Reflection fallback dependency pruning now tracks local shadowing in nested blocks, closures, match arms, and for-loop patterns, so dropping a generated reflect fallback local does not remove unrelated code that reuses the same local name in an inner scope. |
| duplicated structural self-field visitors | `structural_helpers/self_fields.rs` now owns self-field detection and direct self-field collection shared by fmt flush metadata and reflection fallback pruning. |
| string-encoded `& mut pp` resolver helper matching | Resolver structural-helper injection is split by responsibility, and impl self-type checks now use explicit `syn::Type` matching instead of a rendered self-type string. |
| inline runtime primitive post-prune replacement in `compiler/mod.rs` | Reflect, `os.Stdout`, and `sync.Pool` replacement policy now lives in `compiler/runtime_primitives.rs`, leaving the main compiler pipeline responsible for orchestration and module pruning. |
| mixed runtime primitive replacements in `runtime_primitives.rs` | The runtime primitive dispatcher now delegates reflect, os, and sync replacement bodies to focused modules under `compiler/runtime_primitives/`. |
| duplicated runtime primitive dispatcher names | Compiler and resolver runtime primitive child modules now own their import-path and root/type symbol constants, while parent dispatchers route through those module-owned contracts instead of repeating string literals. |
| duplicated runtime primitive replaced-item pruning | Runtime primitive modules now share one pruning helper for replaced item names, replaced impl receiver types, and dead-code preservation on unrelated generated items. |
| whole-module `sync.Pool` runtime primitive replacement | The sync runtime primitive now replaces only the generated `Pool` type and Pool impls, preserving unrelated reachable sync items in the compiled stdlib module. |
| whole-module `reflect.Value` runtime primitive replacement | Reflect runtime primitive replacement now swaps only generated `Value`-dependent items and preserves unrelated compiled reflect items such as constants, named types, and functions. |
| inline preserved `reflect` stub insertion in `compiler/mod.rs` | Missing preserved reflect module materialization now lives in the reflect runtime primitive module; the main pipeline only computes preserved modules and prunes. |
| inline builtin root expansion in `compiler/mod.rs` | Builtin DCE root expansion for channels, pointer cells, and reflect helpers now lives in `compiler/builtin_roots.rs`; module pruning delegates to that explicit dependency table. |
| inline `reflect.TypeOf(...).Kind()` detector in `compiler/mod.rs` | Reflect-kind comparison detection, argument extraction, and kind mapping now live in `compiler/reflect_kind.rs`; binary expression lowering only emits the resulting builtin check. |
| inline reflect slice-to-`any` writeback lowering in `compiler/mod.rs` | Reflect-backed slice writeback planning and call lowering now live in `compiler/reflect_slice_any.rs`; ordinary call lowering delegates only when the focused module detects an addressable slice passed to an `any` parameter. |
| duplicated reflect slice writeback planning in `reflect_slice_any.rs` | Writeback eligibility, parameter types, and addressable slice arguments now live in one explicit plan consumed by both detection and lowering. |
| bare method-name retention in inherent impl DCE | Inherent impl items are now retained only through receiver-qualified `Type::method` reachability names, so a reachable top-level function or trait item named `String` no longer keeps unrelated `String` methods on reachable private receiver types. |
| missing receiver-qualified roots for unknown-base field method calls | DCE reference collection now falls back from `unknown.field.method(...)` to the generated struct field-type table, rooting `FieldType::method` without restoring broad bare method-name retention. |
| rendered-token `.lock().unwrap()` receiver detection in `compiler/mod.rs` | Scoped method receiver temporaries are now selected through shared `compiler/syn_inspect.rs` AST detection of the `lock().unwrap()` method-call chain instead of searching formatted Rust source text. |
| rendered-token `Box::new(()) as Box<dyn Any>` matching in `compiler/mod.rs` | Empty-interface zero-value detection now matches the cast and `Box::new(())` call through shared `compiler/syn_inspect.rs` AST helpers, so formatting and whitespace no longer control package-static initialization behavior. |
| duplicated boxed-any expression inspection | Boxed-empty-interface zero detection and boxed-any cast-source extraction now live in `compiler/syn_inspect.rs`; package-static lowering consumes the shared AST predicates and retargets existing boxed-any initializers to `Box<dyn Any + Send + Sync>` instead of carrying local cast/method-call matching. |
| rendered-token fallback in numeric receiver self detection | Receiver `self` and direct `self.field` detection now uses shared `compiler/syn_inspect.rs` AST shape predicates; unrelated expressions no longer fall back to formatted-token equality. |
| rendered-token `Any` trait-bound matching in `compiler/mod.rs` | `Box<dyn Any>` recognition now uses shared `compiler/syn_inspect.rs` trait-object bound matching instead of comparing formatted trait-bound paths. |
| rendered-token same-lvalue RHS matching in `compiler/mod.rs` | `take_rhs_lvalue_reads` now uses shared `compiler/syn_inspect.rs` structural expression/type/path matching for assignment targets and RHS reads instead of rendered expression strings. |
| rendered-token type-parameter bound dedupe in `compiler/mod.rs` | Generic constraint bound dedupe/append/ensure helpers now compare `syn::TypeParamBound` structure through shared `compiler/syn_inspect.rs` bound/path/type matching instead of formatted bound strings. |
| rendered-token interface implementor type dedupe in `compiler/mod.rs` | Interface implementor type lists now dedupe through shared `compiler/syn_inspect.rs` structural `syn::Type` list matching instead of formatted type strings. |
| rendered-token `#[allow(dead_code)]` detection | Generated dead-code preservation now lives in `compiler/generated_attrs.rs` and parses attribute metadata instead of searching the formatted attribute token stream. |
| rendered-token post-merge interface helper dedupe in `compiler/mod.rs` | Noop supertrait helper insertion now compares generated impl trait/self targets through shared `compiler/syn_inspect.rs` structural matching instead of filtering by formatted item tokens. |
| runtime interface hook policy mixed into `compiler/mod.rs` | Runtime hook names, hook-name comparisons, trait-path classification, clone-box impl generation, noop hook detection, and missing clone-hook patching now live in `compiler/interface_hooks.rs`; compiler lowering and reachability consume that generated-interface contract through one focused boundary. |
| interface method-set discovery mixed into `compiler/mod.rs` | Direct/imported/embedded interface method expansion, needed-import AST scanning, and value/pointer satisfaction predicates now live in `compiler/interface_method_sets.rs`; the compiler root consumes method-set facts when orchestrating impl emission. |
| imported interface impl orchestration mixed into `compiler/mod.rs` | Imported interface filtering and local-struct pointer impl emission now live in `compiler/imported_interface_impls.rs`; the compiler root only passes collected method/generic/emitted-impl state. |
| concrete interface impl assembly mixed into `compiler/mod.rs` | Concrete interface impl bodies and promoted embedded-method forwarding now live in `compiler/interface_impls.rs`; the compiler root keeps only interface satisfaction and impl-orchestration decisions. |
| pointer interface impl assembly mixed into `compiler/mod.rs` | Pointer-cell and borrowed-pointer interface impl bodies now live in `compiler/interface_impls.rs`; the compiler root only decides which local or imported interface impls should be emitted. |
| noop interface item assembly mixed into `compiler/mod.rs` | Noop interface structs, noop trait impl bodies, and noop supertrait impl traversal now live in `compiler/noop_interfaces.rs`; TypeEnv-derived external interface method signatures and Rust-path name recovery live in `compiler/interface_type_env.rs`. |
| embedded interface impl assembly mixed into `compiler/mod.rs` | Borrowed embedded-interface value and pointer impl generation now lives in `compiler/embedded_interfaces.rs`; the compiler root only supplies collected embedded fields and method maps. |
| struct manual derive/default/clone policy mixed into `compiler/mod.rs` | Struct field derive facts, manual `any` clone expressions, and aggregate manual Default/Clone/Send/Sync decisions now live in `compiler/struct_derives.rs`; type-spec lowering only records emitted fields and consumes the resulting policy. |
| Go zero/default value construction mixed into `compiler/mod.rs` | Default expressions for `any`, `error`, interface, function, and fixed-array types now live in `compiler/zero_values.rs`; declaration, struct, array, and goto-hoist lowering consume one shared zero-value contract. |
| duplicate Go/Rust zero-value fallback helpers in `compiler/mod.rs` | `compiler/zero_values.rs` now owns optional AST-type, simple Go type-name, inferred `GoType`, and lowered Rust `syn::Type` zero expressions; named returns, package vars, struct literal completion, and declaration fallbacks all call that boundary. |
| duplicate receiver-method target maps in `compiler/mod.rs` | Mutable-reference argument borrowing and clone/value argument normalization now share `compiler/receiver_method_targets.rs`, a generic receiver-qualified target table with the same unambiguous-method fallback semantics, instead of maintaining parallel map implementations in the compiler root. |
| duplicated receiver-type scope tracking in argument rewrite visitors | Mutated-slice borrowing, mutable-reference argument borrowing, and clone/value argument normalization now share `compiler/receiver_type_scopes.rs`, so function/impl/block scopes, typed parameter bindings, local receiver facts, and current `self` receiver state are recorded through one boundary outside the compiler root. Scope-local binding helpers live with that tracker rather than in `compiler/mod.rs`. |
| duplicated receiver-scope visitor boilerplate in generated call-argument rewrites | Mutated-slice borrowing, mutable-reference argument borrowing, and clone/value normalization now share one receiver-scoped `VisitMut` wrapper; each pass keeps only its own call-rewrite policy. |
| generated call-argument rewrite subsystem mixed into `compiler/mod.rs` | Mutated-slice borrowing, mutable-reference argument borrowing, clone/value normalization, and vec-newtype receiver restoration now live in `compiler/call_arg_rewrites.rs`; the compiler root only invokes the pipeline entrypoints. |
| phantom type-parameter field injection mixed into `compiler/mod.rs` | Unused generic struct parameter detection, generated `PhantomData` fields, and matching struct-literal completion now live in `compiler/phantom_type_params.rs`; the compiler root only invokes the transform. |
| Go string/byte literal handling mixed into `compiler/mod.rs` | Go escape decoding, byte-preserving string literal conversion, string-constant byte helpers, and `[]byte(string)` expression lowering now live in `compiler/go_strings.rs`; const lowering and expression lowering consume that boundary. |
| named-return rewrite and state mixed into `compiler/mod.rs` | Named result idents, synthetic exit-label counters, explicit-return-to-break rewriting, named-result declarations, and final named-result expression construction now live in `compiler/named_returns.rs`; function, method, assignment, and range-function lowering consume that boundary. |
| loop-control continue rewrites mixed into `compiler/mod.rs` | Synthetic loop-body label generation, range-loop continue labeling, and Go `for` post/per-iteration tail rewriting now live in `compiler/loop_control.rs`; range and `for` lowering request corrected Rust loop bodies instead of carrying syn visitors. |
| synthetic name counters mixed into `compiler/mod.rs` | Defer, switch, select, goto-state, range-function, and unnamed-argument generated names now live in `compiler/synthetic_names.rs`; the shared lowering reset also resets range-function names instead of leaving that counter outside the reset path. |
| active local/item name state mixed into `compiler/mod.rs` | Local-vs-item collision tracking, scoped active-local guards, value identifier renames, and shadowed-predeclared checks now live in `compiler/active_names.rs`; compiler lowering keeps only AST source-map recording for local binding idents. |
| current receiver context mixed into `compiler/mod.rs` | Current method receiver storage, scoped receiver guards, receiver-name checks, and pointer-receiver type lookup now live in `compiler/current_receiver.rs`; unsafe/raw-pointer lowering consumes the context without owning thread-local state. |
| import context state mixed into `compiler/mod.rs` | Default import package names, current-file import names, import-path matching, alias rewrites, dot-import rewrites, and Rust module-name rewriting now live in `compiler/import_context.rs`; compiler lowering consumes import facts through named predicates and path builders. |
| package lowering context mixed into `compiler/mod.rs` | Current Go package-name qualification and main-package package-var lowering mode now live in `compiler/package_context.rs`; file lowering sets scoped guards while expression/interface lowering consumes named context queries. |
| receiver-type fact extraction mixed into `compiler/mod.rs` | Receiver refs/maps, top-level receiver facts, and Rust type/expression receiver inference now live in `compiler/receiver_type_facts.rs`; DCE/ref collection and argument-rewrite orchestration consume that boundary instead of owning the fact logic. |
| duplicated DCE reachability fingerprint serialization in `compiler/mod.rs` | Module fixpoint detection and reachable-item cache keys now share one length-delimited reachability fingerprint helper, leaving token serialization only as an explicit cache input. |
| item-level DCE filtering mixed into `compiler/mod.rs` | Reachable item cloning/filtering, impl/trait item retention, self-reachability naming, and type/path mention predicates now live in `compiler/item_reachability.rs`; the main compiler module keeps the fixpoint orchestration and reference collection boundary. |
| shared `syn` inspection helpers mixed into item reachability | Item/name, self-type, path-prefix, macro-token, and type/path mention scanners now live in `compiler/syn_inspect.rs`; reachability and display pruning both consume the shared AST-query boundary. |
| exact path-call inspection mixed into `compiler/mod.rs` | Exact `syn::Path` and `syn::Expr::Path` call-target matching now lives in `compiler/syn_inspect.rs`; compiler lowering, receiver-type facts, display pruning, and item reachability share that AST query instead of open-coding segment comparisons. |
| generated function call-target key parsing in `compiler/mod.rs` | Generated call-argument passes now use `compiler/syn_inspect.rs::call_target_key`, so local and module-qualified function target normalization lives with the shared AST path inspection helpers. |
| duplicated constructor call-target inspection | Named numeric constructor detection now uses `compiler/syn_inspect.rs` call-expression last-path-segment inspection after compiler-root Rust-safe Go name mapping, instead of open-coding `syn::Expr::Call` path matching. |
| duplicated expression path/self inspection | Exact path identifiers, path identifiers through generated paren/group/deref wrappers, exact `self` detection, single-segment type-path names, and paren/group stripping now live in `compiler/syn_inspect.rs`; compiler-root rewrites and coerce-types passes consume one AST-query boundary. |
| duplicated exact expression identifier inspection | Exact unqualified `syn::Expr` identifier matching now lives in `compiler/syn_inspect.rs`; named-return lowering consumes that shared AST query instead of carrying a local path matcher. |
| duplicated nil-check path inspection | The nil-check pass now uses `compiler/syn_inspect.rs` exact path-ident extraction for generated `None` detection instead of carrying a local single-segment path matcher. |
| owned `to_string()` reference call-argument cleanup | The generated-Rust cleanup for `&(value.to_string())` call arguments was removed; string ownership now comes from source expected-type lowering instead of repairing borrowed owned strings after codegen. |
| duplicated zero-arg method-call inspection | Generated zero-arg method receiver matching now lives in `compiler/syn_inspect.rs`; clone, to_vec, and UnsafePointer rewrites consume that shared AST query instead of open-coding method name plus empty-arg checks. |
| duplicated receiver wrapper method-name inspection | Generated receiver wrapper method predicates for clone/lock/unwrap and lock/unwrap guard chains now live in `compiler/syn_inspect.rs`; receiver type facts, DCE reference collection, and mutable-borrow analysis consume the same named AST contract instead of stringifying method identifiers locally. |
| duplicated generated wrapper type-argument inspection | Generated wrapper type argument extraction for Arc, Box, Mutex, and GorsPtr paths now lives in `compiler/syn_inspect.rs`; named self-type recovery and receiver type facts consume that shared AST helper instead of matching last path segments locally. |
| duplicated receiver-scope fact setup in generated call-argument passes | `receiver_type_scopes::ProgramFacts` now owns module names, item names, return-type facts, and field-type facts for generated Rust receiver tracking; clone-value, mutable-reference, and mutated-Vec argument passes request trackers from that boundary instead of rebuilding the same context independently. |
| missing supertrait propagation for mutable-reference method arguments | Mutable-reference argument rewriting now inherits method signatures through generated trait supertraits, so embedded-interface receivers use the same semantic method target propagation as clone-value argument rewriting. |
| duplicated supertrait scanning in generated call-argument target collectors | Clone-value and mutable-reference method target collection now share one generated-trait supertrait recorder, keeping embedded-interface propagation in one receiver-method target boundary. |
| under-recorded trait methods in mutable-reference argument fallback | Mutable-reference method target collection records same-named trait methods even when they have no mutable-reference parameters, so unknown receiver calls do not borrow arguments from an ambiguous method-name fallback. |
| duplicated method-argument index shifting in generated call-argument target collectors | Clone-value and mutable-reference method target collection now share one `method_arg_targets` helper for converting signature parameter targets to method-call argument targets after the receiver slot. |
| duplicated borrowed-pointer path inspection | Borrowed-pointer argument rewriting now uses `compiler/syn_inspect.rs` exact path-ident extraction before consulting compiler-owned borrowed-pointer parameter state, instead of open-coding single-segment `syn::Expr::Path` matching. |
| dead borrowed-pointer call-argument cache | The no-op call-site borrowed-pointer cache and rewrite path were removed. Pointer parameters lower through the explicit `GorsPtr<T>` value model, and pointer receiver borrowed-name state remains scoped to receiver dereference lowering instead of driving unreachable call-argument rewrites. |
| duplicated slice-range index inspection | Slice range-index expression detection now lives in `compiler/syn_inspect.rs`; clone/value argument normalization consumes the shared AST predicate when materializing slice arguments as `Vec<T>`. |
| duplicated generated self-shape inspection | Exact `self`, reference-wrapped `self`, and direct self-field predicates now live in `compiler/syn_inspect.rs`; remaining generated Rust receiver checks consume the same structural AST queries. |
| generated self-pointer comparison postpass | Current pointer receiver `b.field == b`/`!=` lowering now uses Go receiver and field type facts while lowering binary expressions, so generated Rust is no longer swept later to cast `self` in direct self-field comparisons. |
| duplicated clone-wrapper expression inspection | Direct `.clone()` receiver extraction, root-compatible parenthesized clone receiver extraction, generated wrapper-stripped clone receiver extraction, and path lookup through clone wrappers now live in `compiler/syn_inspect.rs`; compiler-root pointer rewrites and coerce-types pointer-cell analysis consume the same AST-query boundary. |
| duplicated clone-call expression predicate | Clone-call detection over generated paren/group wrappers now lives beside the clone receiver extractors in `compiler/syn_inspect.rs`; append lowering consumes that shared predicate instead of matching method calls locally. |
| generic expression search mixed into evaluation-order hoisting | Receiver-root detection, mutable-borrow path detection, local path containment, and method-call containment now live in `compiler/syn_inspect.rs`; evaluation-order hoisting and pointer-cell iterable analysis consume shared structural readers instead of depending on each other. |
| duplicated generated call-shape inspection | `Box::new` predicates and `std::sync::Arc::new(std::sync::Mutex::new(...))` readers now live in `compiler/syn_inspect.rs`; interface materialization and coerce-types comparisons share the same AST checks. |
| duplicated leaked-box call inspection | `Box::leak(...)` detection now lives with the generated call-shape helpers in `compiler/syn_inspect.rs`; call-argument rewrite paths consume the shared AST predicate instead of matching the call locally. |
| duplicated pattern identifier inspection | Single-binding and multi-binding `syn::Pat` identifier extraction now live in `compiler/syn_inspect.rs`, shared by compiler-root rewrites, receiver-type scope tracking, and coerce-types call/pointer argument analysis. |
| duplicate goto-hoist pattern identifier inspection | Goto-state hoisted-local rewriting now consumes `compiler/syn_inspect.rs` pattern identifier extraction instead of carrying a local `syn::Pat` matcher. |
| duplicated function-argument identifier inspection | Bodyless function fallback generation now consumes `compiler/syn_inspect.rs` typed `syn::FnArg` identifier extraction instead of carrying a local argument-pattern matcher. |
| duplicated Rust type-shape inspection | `Vec<T>`, slice inner type, and first generic type-argument extraction now live in `compiler/syn_inspect.rs`, shared by compiler-root argument rewrites and coerce-types pointer-cell analysis. |
| display impl generation/pruning mixed into `compiler/mod.rs` | Display impl creation for value `String() string` methods and retention now live in `compiler/display_impls.rs`, where inherent `String` methods and `std::error::Error` impls explicitly define which generated Display impls remain reachable. |
| suffix-only Display/Error trait matching in display impl pruning | Display pruning now matches exact `std::fmt::Display` and `std::error::Error` paths, so unrelated traits named `Display` or nested error-like paths are not treated as generated display glue. |
| path-specific `crate::fmt::Stringer` DCE pruning | External trait impl admission now requires an explicit trait root unless the impl belongs to a semantic generated category: unqualified non-local Rust traits, standard/runtime error traits, `std::fmt::Display`, `std::ops` newtype operators, `crate::builtin` runtime traits, host-resource traits, or pointer-cell imported-interface shims. |
| inline resolver structural-helper injection in `resolve/mod.rs` | Resolver-owned generated helper insertion now lives in `resolve/structural_helpers.rs`, so package resolution no longer owns the helper predicates and injected item bodies inline. |
| mixed resolver structural-helper policies | Noop interface sentinels, mutable-reference trait forwarding, and fmt flush hook injection now live in focused modules under `resolve/structural_helpers/`; the parent module keeps dispatch, facts, and shared AST predicates. |
| inline resolver runtime primitive module generation in `resolve/mod.rs` | Synthetic `runtime` and `internal/reflectlite` module bodies now live in `resolve/runtime_primitives.rs`, leaving `resolve/mod.rs` responsible for resolver orchestration and caching. |
| mixed resolver runtime primitive bodies in `resolve/runtime_primitives.rs` | The resolver runtime primitive dispatcher now delegates synthetic `runtime` and `internal/reflectlite` bodies to focused modules under `resolve/runtime_primitives/`. |
| rendered-token resolver use-item dedupe in `resolve/mod.rs` | Generated `use` items now dedupe through structural `syn::UseTree` matching, so formatting changes do not control resolver output. |
| literal `reflect` qualifier in reflect-kind detection | `compiler/reflect_kind.rs` now receives an import-path predicate from current-file import facts, so aliased imports such as `import r "reflect"` lower through the same runtime primitive. |
| duplicated compiler-side reflect runtime symbols | `compiler/reflect_semantics.rs` now owns reflect module/member constants and generated Rust reflect path parsing shared by reflect-kind lowering and reflection fallback pruning. |
| literal `printArg` / `printValue` fmt flush triggers | Flush insertion now derives trigger methods from the generated hook's source field and the receiver's self-call graph, so arbitrary method names only flush when they can write through the buffered source field. |
| broad `take` call matching in fmt flush source detection | Flush-source metadata now recognizes the generated `std::mem::take` call shape instead of treating any call path ending in `take` as a source-buffer extraction. |
| initial-pass fmt flush insertion | The initial `coerce_types` visitor no longer performs fmt flush insertion from preexisting hooks; the post-structural-helper pass is the only flush insertion owner after resolver helper injection has completed. |
| initial-pass reflection fallback pruning | The initial `coerce_types` visitor no longer deletes `reflect::ValueOf`/`TypeOf` or `reflect.Value` paths. Generated reflection fallback pruning now stays behind the structural-helper boundary instead of sweeping ordinary local or main-package code. |
| hardcoded `State for &mut pp` resolver helper body | Resolver structural helpers now derive mutable-reference `State` forwarding impls from existing named `impl State for T` blocks and the actual trait method signatures, so the forwarder is no longer tied to `pp` or a fixed method list. |
| hardcoded fmt noop interface impl bodies | Resolver noop interface helpers now derive injected no-op method impls from the actual generated trait signatures, including runtime interface hooks, instead of carrying separate Formatter/Stringer/GoStringer bodies. |
| shared `has_pp` resolver structural-helper fact | Resolver fmt flush injection now discovers receiver candidates from struct field shape plus an existing `State` impl inside the fmt-flush helper module instead of carrying a package-specific receiver flag through shared facts. |
| literal `fmt`/`buf` resolver flush hook fields | Resolver fmt flush injection now derives the transfer path from receiver field types, nested byte-buffer storage, and the destination byte-buffer field instead of requiring those fields to be named `fmt` and `buf`. |
| shared resolver structural-helper fact carrier | Noop interface helpers, mutable-reference forwarders, and fmt flush injection now own their own precondition discovery; the resolver structural-helper parent only orchestrates the focused modules. |
| resolver structural-helper AST predicates mixed into dispatcher | Shared trait/type/impl predicates now live in `resolve/structural_helpers/syn_helpers.rs`; the parent module only orchestrates focused helper injection. |
| duplicated resolver trait-method scanners | Resolver structural-helper modules now share one parent `trait_methods` AST helper for generated trait method extraction instead of carrying separate copies. |
| duplicated noop-interface trait facts | Resolver noop interface injection now collects present trait names once and applies the target table against that set instead of mirroring each target as a separate boolean plus string match. |
| hardcoded `State` mutable-reference forwarder injection | Resolver mutable-reference forwarders now scan local generated traits and named-type impls, then derive `&mut T` impls from forwardable trait method signatures instead of selecting the `State` trait by name. |
| `padString` body replacement in `coerce_types.rs` | The generated Go body now compiles through generic method-call and string argument lowering, preserving width-padding behavior instead of replacing the named method with a direct write. |
| `fmtString` body replacement in `coerce_types.rs` | The generated Go body now compiles through generic branch, receiver-method, and string argument lowering, preserving `%q`, `%x`, and `% X` string formatting behavior instead of replacing the named method with `fmtS`. |
| `newPrinter` body replacement in `coerce_types.rs` | The generated Go body now compiles through pointer-cell ownership, generic receiver-method argument hoisting, manual cloning for structs with `any` fields, and a minimal `sync.Pool` runtime primitive instead of replacing the named function body. |
| `free` body removal in `coerce_types.rs` | The generated Go method body is no longer deleted by method name; currently supported pool behavior is handled at the `sync.Pool` runtime boundary. |
| untyped Rust `usize` result from Go `copy` builtin lowering | Builtin `copy` lowering now casts the runtime `copy_slice` count to the generated Go `int` representation (`isize`), so ordinary `w += copy(dst, src)` composes with other Go integer results without package-specific call-site coercions. |
| fmt flush insertion by receiver type name in `coerce_types.rs` | Flush insertion is now gated by generated receiver metadata collected across impl blocks, and a narrow post-structural-helper pass runs after resolver helper injection. Arbitrary receivers named differently or receivers without the hook no longer get name-selected flush calls. |
| `pp` receiver-name gate for `self.value` reflection fallback pruning | The fallback prune now uses generated self-value reflection fallback shapes instead of the literal type name `pp` or the flush hook, so helper ordering does not leave unsupported reflect branches in generated output. |
| hardcoded field-name cloning in the former final-return pass | Tail-return field cloning now derives from borrowed receiver/parameter roots, so borrowed field returns clone regardless of field name and owned field returns are not cloned just because they are named `msg`, `err`, or `errs`. |
| byte-index typed-const comparison postpass | Go binary-expression lowering now casts byte indexes from source/type facts when comparing against a typed integer const, so the fix no longer scans generated Rust signatures or const items and no longer depends on the `unicode/utf8.RuneSelf` name. |
| hardcoded imported interface parameter pass | The stale `interface_param` pass and its literal `Writer` selector rule were removed; current interface parameter ownership comes from compiler/type-env lowering and remains covered by unit tests. |
| name-scanned local trait parameter pass | The stale `trait_param` pass was removed; local interface parameter lowering now comes from the compiler's type environment instead of scanning generated Rust trait item names after codegen. |
| primitive type mapping postpass | The stale `map_type` pass was removed; `syn::Type` conversion now delegates to compiler AST type lowering, so Go primitive type names are mapped before generated Rust postpasses run. |
| primitive type-conversion postpass | The stale `type_conversion` pass was removed; Go type conversion calls lower through IR call ABI and compiler AST lowering instead of rewriting generated Rust calls by primitive type name. |
| nil-check postpass | The stale `nil_check` pass was removed; expected-type nil construction and typed nil comparisons now lower from Go type facts before generated Rust postpasses run. |
| string literal ownership postpass | The broad `string_lit` pass was removed; string literals now become owned `String` values at expected-type declaration, assignment, call, return, composite literal, and string-concat lowering sites instead of sweeping generated Rust syntax. |
| array/slice index cast postpass | The broad `index_cast` pass was removed; Go index and slice lowering already cast index components to `usize` at the semantic source sites, so arbitrary generated Rust indexing is no longer rewritten globally. |
| borrowed-field clone in the former final-return pass | The semantic field clone rewrite was removed from the style pass; non-Copy selector materialization belongs to typed expression/return lowering before final-return cleanup runs. |
| trailing-return style postpass | The stale `simplify_return` pass was removed; final `return expr` cleanup now runs during function/method block finalization after named-result wrapping and missing-return handling. |
| static-false branch pruning postpass | The stale `static_false` pass was removed; Go `if` conditions that constant-evaluate to bool now select the reachable branch during source lowering, and mutable locals initialized to `false` are no longer unsoundly treated as permanently false. |
| tuple-newtype receiver coercion postpass | Named numeric receiver conversions now use scoped Go receiver facts during type-conversion lowering, ordinary named-slice assignments are wrapped by assignment lowering, and named-slice method receiver restoration remains in the cross-module receiver pass. |
| generated equality normalization in `coerce_types/comparisons.rs` | The postpass module was removed after source lowering and expected-type materialization took ownership of interface, pointer-cell, and value construction shapes; generated equality is no longer repaired by matching `Box::new` or `Arc<Mutex>` calls after codegen. |
| single-expression block flattening postpass | The pure cosmetic `flatten_block` pass was removed; generated block structure now comes directly from Go lowering and later semantic passes instead of a global syntax cleanup sweep. |
| generated `gors_channel` runtime injection pass | The stale `inject_channel` pass was removed; channel types and operations now lower to the shared `crate::builtin::Chan` runtime instead of injecting a second per-file channel implementation. |
| synthetic `use` hoisting postpass | The cosmetic `hoist_use` pass was removed; generated cross-module and standard-library paths now stay qualified at their lowering sites instead of being rewritten into top-level imports. |
| module-level semantic external-trait impl rooting | The semantic DCE audit no longer treats any reference to a module or trait name as a root for broad local trait impl bodies; generated noop sentinel impls, preserved impl metadata, and self-reachable runtime/ambient trait impl edges own those impl bodies. |
| external function result method over-rooting in DCE | Token-derived external root collection no longer invents roots such as `AppendFloat::as_bytes` for ordinary Rust methods called on external function results; the function call roots the function, while typed receiver inference owns real returned receiver methods. |
| unqualified semantic member-name expansion | The semantic reachability graph no longer maps ambiguous member names such as `Read` or `Write` to every impl and trait item with that method name; local semantic edges now require top-level names or qualified receiver roots. |
| external generic receiver return erasure | Receiver type facts preserve the first concrete type argument of external receiver types, so calls such as `sync/atomic.Pointer[poolChainElt].Load()` can return the concrete generated receiver instead of the placeholder `T`. |
| always-on strict semantic DCE audit | The strict token-vs-semantic external-root comparison now runs only when `GORS_SEMANTIC_REACHABILITY_AUDIT=1`, keeping the token DCE path authoritative while the semantic graph is still being broadened toward full root-specific parity. |
