name: ci
on: [push, pull_request]

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain-file: rust-toolchain.toml

      - name: Run make lint
        run: make lint

      - name: Run make doc
        run: make doc

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain-file: rust-toolchain.toml

      - name: Run make build-release
        run: make build-release

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain-file: rust-toolchain.toml

      - name: Run make test
        run: make test test-lexer test-parser

  test-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain-file: rust-toolchain.toml

      - name: Install Go toolchain
        uses: actions/setup-go@v5
        with:
          go-version: '^1.21'

      - name: Run make test-integration
        run: make test-integration

  www:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain-file: rust-toolchain.toml

      - name: Install Node.js toolchain
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install wasm-pack
        run: make install-tools

      - name: Build www
        run: make www

      - name: Lint www
        run: make www-lint

      - name: Upload the build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: www-dist
          path: ./gors-www/dist

  # Deploy website on every push to master (nightly)
  deploy-www:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    needs: [lint, build, test, test-integration, www]
    steps:
      - uses: actions/checkout@v4

      - name: Download the build artifacts
        uses: actions/download-artifact@v4
        with:
          name: www-dist
          path: ./gors-www/dist

      - name: Deploy www to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: ./gors-www/dist
          clean: true
